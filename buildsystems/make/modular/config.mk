# HYP: use root config.mk to control/substitute settings of all projects together
#   - different sets of different projects per root
#   - different settings and chosen components for each project per root
#   - different toolchain, deploy method, directory
