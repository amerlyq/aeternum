#!/usr/bin/picolisp /usr/lib/picolisp/lib.l
# OR: #!/usr/bin/pil
# vim:ft=lisp
#%SUMMARY: echo stdin

# (load "@ext.l" "myfiles/lib.l" "myfiles/foo.l")
# (do ... something ...)

# REF: https://rosettacode.org/wiki/Copy_stdin_to_stdout
# copy stdin to stdout
(in NIL (echo))
; (bye)
